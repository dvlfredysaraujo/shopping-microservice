spring:
  application:
    name: DISCOVERY-SERVER

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false

logging:
  level:
    root: INFO
